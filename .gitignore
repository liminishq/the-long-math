# Build output — regenerate with: npm run build
dist/

# Article pages under articles/investing-and-financial-literacy/ are tracked (site.js injects header/footer).
# French hub + articles under fr/articles/investing-and-financial-literacy/ are tracked too (run npm run build, then commit).

# Dependencies
node_modules/

# Environment / secrets (if you add any later)
.env
.env.local
*.local

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
