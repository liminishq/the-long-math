# $160k Calculator vs External Comparator Comparison

Compare **the-long-math** Canada income tax calculator to an external comparator tax tool for **$160,000** in each income type, for every province and territory (2025).

## 1. Generate our calculator’s results

From the **repo root**:

```bash
node tools/tests/run-160k-comparison.js --year=2025 --out=our-results.2025.160k.json
```

This writes `tools/tests/our-results.2025.160k.json` and prints a table: **Province × Income type** with Total tax, Federal, Provincial, Taxable income, Marginal %.

Income types: **Salary**, **Capital gains**, **Eligible dividends**, **Other income**.

## 2. Fill external baseline

1. In your comparator tax tool, set **year 2025** and **$160,000** in exactly one of: Employment income, Capital gains, Eligible dividends, Other income (all others $0).
2. For each **province/territory** and each **income type**, record:
   - **Total tax**
   - **Federal tax**
   - **Provincial tax**
3. Put them in `tools/tests/external-baseline.2025.160k.json` with the **same row structure** as `our-results.2025.160k.json`: each row must have `province`, `incomeType`, `totalTax`, `federalTax`, `provTax`.

Easiest: copy `our-results.2025.160k.json` to `external-baseline.2025.160k.json`, then replace the tax numbers with values from your comparator tax tool (keep `province` and `incomeType` so rows match).

## 3. Run the comparison

From the **repo root**:

```bash
node tools/tests/compare-with-external-baseline.js
```

The script reports rows where the difference (ours − external baseline) exceeds **$5** for total tax, federal, or provincial tax, and exits with code 1 if any such differences exist.

## Files

| File | Purpose |
|------|--------|
| `run-160k-comparison.js` | Runs our calculator for all provinces × 4 income types at $160k; writes JSON and table. |
| `our-results.2025.160k.json` | Our results (generated by step 1). |
| `external-baseline.2025.160k.json` | External comparator results; you fill this (step 2). |
| `compare-with-external-baseline.js` | Diffs our results vs baseline and lists errors (step 3). |

## Provinces / territories included

AB, BC, MB, NB, NL, NS, NT, NU, ON, PE, QC, SK, YT.

**Note:** Quebec (QC) may differ from your comparator tool if that tool uses Revenu Québec–specific rules not yet in our engine.
